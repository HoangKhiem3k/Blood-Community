import { HospitalRoutes } from './HospitalRoutes';
import MainLayout from '../../components/MainLayout';

function HospitalPage() {
    return <MainLayout routes={HospitalRoutes} />;
}

export default HospitalPage;
